import { BsFillCameraReelsFill, BsSearch } from "react-icons/bs";

const Header = () => {
  return (
    <section id="header" className="header-container">
      <h1>Takeaway Challenge MovieDB</h1>

      <BsFillCameraReelsFill className="camera-icon" />
    </section>
  );
};

export default Header;
